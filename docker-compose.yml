services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: finance_agent
    ports:
      - "5432:5432"
    volumes:
      - finance_agent_pgdata:/var/lib/postgresql/data
volumes:
  finance_agent_pgdata:
